import { GeintereseerdAssignment } from './geintereseerd-assignment.model';

describe('GeintereseerdAssignment', () => {
  it('should create an instance', () => {
    expect(new GeintereseerdAssignment()).toBeTruthy();
  });
});
